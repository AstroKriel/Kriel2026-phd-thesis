%% sections use \sref{}; appendix sections use \aref{} (cref can't tell them apart: same counter)
\def\itemautorefname{Step}
\newcommand{\sref}[1]{Section~\ref{#1}}
\newcommand{\aref}[1]{\hyperref[#1]{Appendix~\ref{#1}}}
